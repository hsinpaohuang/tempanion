name: Install & Cache Dependencies

description: Runs setup and yarn install

runs:
  using: composite
  steps:
    - name: Setup
      uses: ./.github/actions/setup

    - name: Install
      run: yarn --frozen-lockfile
      shell: bash
