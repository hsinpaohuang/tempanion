name: Install & Cache Dependencies

runs:
  using: composite
  steps:
    - name: Setup
      uses: ./.github/actions/setup.yml

    - name: Load Cache
      uses: ./.github/actions/load-yarn-cache.yml

    - name: Install
      run: yarn --frozen-lockfile
